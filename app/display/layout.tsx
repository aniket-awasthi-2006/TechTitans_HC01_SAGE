export const metadata = {
  title: 'OPD Token Display | MediQueue',
  description: 'MediQueue: Live OPD token display board',
};

export default function DisplayLayout({ children }: { children: React.ReactNode }) {
  return children;
}
